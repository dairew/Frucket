<div class="jumbotron center">
  <h1>Frucket</h1>
  <% if user_signed_in? %>
    <p> Welcome to Frucket! </p>
  <% else %>
    <p>Use collaborative upvoting to make planning anything a breeze.</p>
    <p><%= link_to "Sign Up", new_user_registration_path, class: "btn btn-primary btn-large" %></p>
    <p><%= link_to "Log In", new_user_session_path, class: "btn btn-primary btn-large" %></p>
  <% end %>
</div>